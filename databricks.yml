# This is a Declarative Automation Bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: bf898149-88c8-4afb-9746-ada7d760be0d

include:
  - resources/*.yml



# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405608753091115.15.azuredatabricks.net
      root_path: /Workspace/Users/nikunjaz2538@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricks_azure_project
      schema: ${workspace.current_user.short_name}
    presets:
      source_linked_deployment: false
  prod:
    mode: production
    workspace:
      host: http://127.0.0.1:7073
      # We explicitly deploy to /Workspace/Users/nikunjaz2538@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/nikunjaz2538@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricks_azure_project
      schema: prod
    permissions:
      - user_name: nikunjaz2538@gmail.com
        level: CAN_MANAGE
